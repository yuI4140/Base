// #![allow(unused)] // For beginning only.
// Made by YuI4140
use crate::prelude::*;
use std::fs::read_dir;

mod error;
mod prelude;
mod utils;

fn main() -> Result<()> {
	println!("Hello, world!");
Ok(())
}
