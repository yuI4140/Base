// #![allow(unused)] // For beginning only.
// Made by YuI4140
use crate::prelude::*;
//use std::fs::read_dir;
mod error;
mod prelude;
mod utils;
fn main() -> Result<()> {
	let path ="./text.txt".to_string();
	let file =TextFormat::new();
Ok(())
}
